apiVersion: v1
kind: ServiceAccount
metadata:
  name: home-coordination
  namespace: home-coordination
  # No annotations needed — the ESO SecretStore references this SA by name.
  # Vault's Kubernetes auth method will validate the SA JWT automatically.
